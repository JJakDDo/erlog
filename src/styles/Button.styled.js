import styled from 'styled-components';

const Button = styled.button`
  background: #fff;
  color: #666;
  border: 0;
  cursor: pointer;

  &:active {
    color: #000;
  }
`;

export default Button;
